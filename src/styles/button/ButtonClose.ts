import styled from 'styled-components';

export const ButtonClose = styled.button` 
  position: absolute;
  top: 5px;
  right: 15px;
  font-size: 36px;
  cursor: pointer;
  transform: rotate(45deg);
`